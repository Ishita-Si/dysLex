# Domain Audit Figures

Binary figure files are intentionally not committed.

Run the domain-shift audit to regenerate histogram/KDE and boxplot PNGs locally:

```bash
python -m src.mvp.domain_audit
```

Generated figures will be written to:

```text
reports/figures/mvp/domain_audit/
```

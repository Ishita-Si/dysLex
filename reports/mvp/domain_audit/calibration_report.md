# Probability Calibration Report

Calibration is evaluated only when trained artifacts and real validation rows are available.

- **reading**: `skipped_missing_artifacts` — Calibration fitting is intentionally skipped unless real validation rows and trained artifacts are present; this audit must not blindly retrain or refit models.
- **writing**: `skipped_missing_artifacts` — Calibration fitting is intentionally skipped unless real validation rows and trained artifacts are present; this audit must not blindly retrain or refit models.
- **typing**: `skipped_missing_artifacts` — Calibration fitting is intentionally skipped unless real validation rows and trained artifacts are present; this audit must not blindly retrain or refit models.
- **fusion**: `skipped_missing_artifacts` — Calibration fitting is intentionally skipped unless real validation rows and trained artifacts are present; this audit must not blindly retrain or refit models.